class Solution{
  public:
    int MissingNumber(vector<int>& array, int n) {
     
      int total = (n) * (n +1)/ 2;
    for (int i = 0; i < n; i++)
    {
        total -= array[i];
    }
    return total;
    }
};
